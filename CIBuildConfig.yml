apiVersion: v1
kind: BuildConfig
metadata:
  name: cipipeline
  labels:
    name: cipipeline
  annotations:
    pipeline.alpha.openshift.io/uses: '[{"name": "camelspringboot", "namespace": "", "kind": "DeploymentConfig"}]'
spec:
  triggers:
    -
      type: GitHub
      github:
        secret: secret101
    -
      type: Generic
      generic:
        secret: secret101
  runPolicy: Serial
  source:
    type: None
  strategy:
    type: JenkinsPipeline
    jenkinsPipelineStrategy:
      jenkinsfile: "node('maven') {\nstage 'build'\nopenshiftBuild(buildConfig: 'camelspringboot', showBuildLogs: 'true')\nstage 'deploy'\nopenshiftDeploy(deploymentConfig: 'camelspringboot')\nopenshiftScale(deploymentConfig: 'camelspringboot',replicaCount: '2')\n}"
  output:
  resources:
  postCommit:
